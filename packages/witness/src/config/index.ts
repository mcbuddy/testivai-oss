/**
 * TestivAI Configuration
 */

export {
  loadLocalConfig,
  createDefaultConfig,
  localConfigExists,
  isLocalMode,
  getConfigPath,
  getDefaultConfig,
} from './local-config';

export type { LocalConfig } from './local-config';
