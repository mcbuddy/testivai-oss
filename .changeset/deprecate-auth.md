---
"@testivai/witness": patch
---

`testivai auth` now prints a deprecation notice: TestivAI is local-first and the hosted service is discontinued — no account or API key is needed. The command still works (`--delete` clears stored credentials) and will be removed in the next major release.
