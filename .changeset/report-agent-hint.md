---
"@testivai/witness": patch
---

The HTML report sidebar now points AI-agent users at `@testivai/mcp` (including the new `explain_snapshot` tool) instead of a hosted-service upsell, and the noise-reduction tips mention `mode: "collapse"` for variable-height regions.
