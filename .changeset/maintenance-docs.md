---
"@testivai/witness": patch
---

Adds a "Maintenance & roadmap" documentation page answering what a team evaluating a dependency actually needs to know: who maintains it, the real release cadence, where the public roadmap is, and precisely what happens to your setup if maintenance stops. Also refreshes ROADMAP.md, which still described Ruby as sidecar-only and MCP image downscaling as upcoming.
