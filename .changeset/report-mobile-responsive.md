---
"@testivai/witness": patch
---

The HTML report is now mobile-friendly: on narrow screens (≤900px) the sidebar becomes an off-canvas drawer — collapsed by default behind a hamburger top bar — the baseline/diff/current images stack vertically, and long commands/selectors wrap instead of overflowing. Desktop layout is unchanged.
